apiVersion: v1
kind: Service
metadata:
  name: order-service-nodeport
  namespace: dev
spec:
  selector:
    app: order-service
  type: NodePort
  ports:
    - port: 3002
      targetPort: 3002
      nodePort: 30002