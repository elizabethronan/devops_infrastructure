apiVersion: v1
kind: Service
metadata:
  name: database-nodeport
  namespace: dev
spec:
  selector:
    app: database
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30003