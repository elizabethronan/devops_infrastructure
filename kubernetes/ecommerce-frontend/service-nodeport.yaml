apiVersion: v1
kind: Service
metadata:
  name: ecommerce-frontend-nodeport
  namespace: dev
spec:
  selector:
    app: ecommerce-frontend
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30000